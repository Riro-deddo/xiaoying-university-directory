# 数据纠错与贡献

最简单的纠错方式是在 GitHub 提交 Issue，附上大学名称、官方来源 URL、你看到的问题和访问日期。不要提交个人申请材料、账号信息或大学文件的完整复制品。

## 可以接受的来源

- 大学官方域名下、无需登录即可访问的网页；
- 大学官方域名直接发布的 PDF 等公开文件；
- 中国教育主管部门或院校官网提供的规范中文校名、历史名称或更名说明。

论坛、社交媒体、留学机构二手总结、搜索引擎摘要和无法核验的录取判断不能作为事实来源。不要绕过验证码、登录、付费墙、robots 限制或持续的技术拦截。

## 新增或修改大学官方来源

1. 确认大学已经存在于 `src/data/qs-2027-top-200-uk.json` 和 `src/data/universities.json`。
2. 在 `src/data/sources.json` 登记官方来源，明确 `kind`、适用范围 `scope`、中文范围说明和申请季（官网明确时）。
3. 不确定页面能否稳定解析时使用 `link-only`；不要为了增加名单数量而猜测选择器。
4. 启用解析时，在 `tests/fixtures/sources/` 添加只覆盖必要结构的最小测试样本，不保存完整网页或整份大学文件。
5. 为解析器添加正常、空结果和结构变化测试，并设置保守的解析保护条件：最小/最大记录数和最大删除比例。
6. 把来源 ID 加入对应大学的 `sourceIds`，运行数据校验和覆盖报告。

自动任务只会改写 `status.json`、通过保护条件的 `generated/requirements.json` 和由它重建的 `generated/reverse-index.json`；大学说明、来源范围和中文摘要仍由贡献者明确维护。

## 新增中国本科院校或院校别名

1. 规范中文名必须能由教育主管部门或院校官方页面核验，英文名优先使用院校官方写法。
2. `aliases` 只保存可解释的官方来源写法、历史名称或常见规范变体；不要把模糊搜索结果直接写成别名。
3. 先运行院校搜索测试，确保别名不会同时指向两所学校；别名冲突必须报错，不能凭相似度自动合并。
4. 如果两个大学来源使用不同拼写，应合并为一个规范院校记录，并把另一种来源拼写保留为别名。

## 文案边界

- 可以写“官方名单中找到”“公开 List 中暂未找到”“当前未发现可核验的公开 List”；
- 不可以写“可以申请”“不能申请”“保底”“冲刺”或未经官方来源支持的录取结论；
- 部分学院、专业或课程的要求必须显示适用范围，不能外推到整所大学；
- Band、Tier 和大学自己的 List 分组保留官方原称，不翻译成中国院校等级。

## 提交前验证

```bash
pnpm test:run
pnpm build
node scripts/report-source-coverage.mjs
```

如果修改了解析器，还要用对应的最小测试样本运行聚焦测试。所有自动生成文件必须可复现，`git diff` 中不应出现未解释的名单大量删除或跨大学事实。

## 硕士奖学金入口年度复核

每个申请季开始前，重新打开四个 `docs/research/masters-scholarship-entry-batch-*.md` 文件中的每一行，核对最终 URL、硕士范围（或未发现公开入口的否定证据结论）、页面标题、两个身份锚点和 `reviewedAt`，并同步更新分组与证据的 `reviewedAt`。每一行的两条身份锚点还必须由审查者显式更新到 `tests/fixtures/masters-scholarship-anchor-approvals.json`；该文件的键集必须与四批研究行完全相等，不能在测试运行时自动生成或默认接受新锚点。所有生产入口按 `new URL(url).href` 规范化后必须在 101 所大学之间全局唯一。对每所“未发现公开硕士奖学金入口”大学重新搜索是否出现合格的公开入口。完成后重跑四个 `tests/masters-scholarship-entry-batch-{1,2,3,4}.test.ts` 测试，通过常规 Pull Request 提交更新；严禁将一次日常观测自动提升为已接受的证据或入口变更。
